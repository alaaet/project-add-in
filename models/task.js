// Initializing a constructor function
function Task(id, name, duration, start, finish) {
  this.id = id;
  this.name = name;
  this.duration = duration;
  this.start = start;
  this.finish = finish;
}
